v.122: compact deck-picker on mobile + reset-to-anchor on rotation

Deck-picker on small viewports (<=900px):
- Hide the redundant dropdown (it was duplicating the button row).
- Keep the brass-bordered pill design of the 11-deck button row but
  shrink each button: min-width 26px (was 32), padding 4x5 (was 5x6),
  font 0.62rem (was 0.78), border-radius 5, no letter-spacing. The
  whole row now reads as a single dense strip near the top of the
  deck plans view instead of dominating the page.
- Inlined directly into ship.html (same critical-CSS block as v.121)
  so iOS Safari can't serve a cached styles.css that misses the rule.

Rotation reset:
- snapToDeckchart() now jumps the document scroll to (0,0)
  synchronously (instant) before bringing the deckchart back to the
  top of the viewport. Any prior scroll position from the previous
  orientation is wiped, so the user lands at the very top of the new
  view's anchor every time.
- Also resets the deck info column's internal scroll position.

Cache buster bumped to ?v=122 on styles.css + app.js.
