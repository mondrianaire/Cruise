v.129: consolidated deck-plan image sizing (3-pass audit)

PASS 1 - IDENTIFY:
  All 11 deck PNGs are 320x1449 (4.53:1 portrait).  Six conflicting
  CSS rules cap image height at different values: 520px, 640px,
  720px, 74vh, 78vh, 88vh.  Lower values were winning on most
  viewports — especially `.fp-deck-img{max-height:74vh}` was capping
  the IMG element 14vh below the .fp-img-stage 88vh container,
  leaving vertical real estate empty.

  Hotspot coverage:  Decks 1, 2, 3, 9, 10, 11 are fully populated.
  Decks 4, 5, 6 deliberately have no .fp-hot entries (cabin-only
  decks).  Deck 5 uses .fp-img-pin pins for the group cabins
  K5155 / K5161 / K5165 - the correct primitive for cabins.

PASS 2 - VERIFY:
  Deck 9 hotspots line up cleanly with deck-09.png labels
  (Fitness, Spa & Salon, Hydropool, Lido Pool, Lido Bar, Dive-In,
  Canaletto, Lido Market, NY Pizza, Sea View Bar, Sea View Pool).
  Cabin pins on Deck 5 sit at top:82-84% - approximately on the
  KS5155/KS5161/KS5165 row on the deck plan.

PASS 3 - IMPLEMENT:
  - Removed conflicting rules:
      .fp-img-stage{max-height:520px;}
      .fp-img-stage{max-width:520px;max-height:720px;}
      .fp-img-stage{max-height:78vh;}
      .fp-deck-img{max-height:74vh;width:auto;margin:0 auto;}
  - Added single authoritative rule (and mirrored inline for cache
    resistance):
      .fp-img-stage{ max-height: 88vh !important; overflow: hidden; }
      .fp-deck-img{
        max-height: 88vh !important;
        height: auto; width: auto; max-width: 100%;
        object-fit: contain;
      }
  - On desktop view-floor, .fp-plan-col gets height:88vh so the
    sticky column matches the image, no wasted vertical space.

Cache buster -> ?v=129.
