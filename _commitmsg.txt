v.127: drop redundant ship-page titling, tighten mobile header

Before, the top of the ship page stacked SIX layers of titling:
  1. "ms Zuiderdam" kicker
  2. "The Ship & Your Package" h1
  3. page-lead description
  4. sr-only h2 "The Ship — ms Zuiderdam"
  5. "Explore the ship" h3
  6. "Two views — toggle..." dc-sub

Removed the redundant pair from the deckchart head (5 + 6) - the
view-tab pills (Cross-section | Deck plans) and the tap-hint pill on
the SVG already communicate exactly what those two lines said. The
deckchart-head now contains only the Night Owl button (cross-section
view) and the view tabs.

On mobile (<=900px) further tightened the page hero:
  - kicker font 0.6rem (was 0.78ish)
  - h1 font 1.4rem
  - page-lead "Vista-class ship details, dining..." hidden entirely
  - hero padding trimmed top:32px bottom:18px

The relocated "The Ship — ms Zuiderdam" section header + 4 Vista-class
fact cards still appear AFTER the views (v.107), so a user scrolling
down still gets the descriptive context - just not crammed above the
hero views.

Cache buster -> ?v=127.
